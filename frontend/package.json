{
  "name": "market-scanner-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "playwright test",
    "format": "prettier --write src tests *.js index.html package.json",
    "format:check": "prettier --check src tests *.js index.html package.json"
  },
  "dependencies": {
    "lightweight-charts": "^5.2.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@vitejs/plugin-react": "^4.3.4",
    "prettier": "^3.9.6",
    "vite": "^6.4.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
